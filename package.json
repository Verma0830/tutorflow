{
  "name": "tutorflow",
  "version": "1.0.0",
  "description": "TutorFlow Attendance & Fees Tracker App",
  "main": "app.js",
  "scripts": {
    "build": "node build.js",
    "cap:sync": "npm run build && npx cap sync",
    "cap:open": "npx cap open android",
    "cap:sync:android": "npm run build && npx cap sync android"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/cli": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  }
}
